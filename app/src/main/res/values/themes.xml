<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatVista" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary</item>
        <item name="android:background">@color/white</item>
    </style>
    <style name="WithActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryDark">@color/royalBlue</item>
         <item name="background">@color/white</item>
    </style>
    <style name="Theme.ChatVista" parent="Base.Theme.ChatVista" />
</resources>